function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

import PropertyResponse from '../PropertyResponse';
import Property from '../Property';
import { dateDecoder } from '../helpers';

var VehicleTimeResponse = function (_PropertyResponse) {
  _inherits(VehicleTimeResponse, _PropertyResponse);

  /**
   * @property {Date} vehicleTime (date) Vehicle time
   *
   * @example VehicleTimeResponse
    {
      vehicleTime: 2018-02-17T12:05:02.000Z
    }
   */
  function VehicleTimeResponse(data) {
    _classCallCheck(this, VehicleTimeResponse);

    var _this = _possibleConstructorReturn(this, (VehicleTimeResponse.__proto__ || Object.getPrototypeOf(VehicleTimeResponse)).call(this));

    var properties = [new Property(0x01, 'vehicleTime').setDecoder(dateDecoder)];

    _this.parse(data, properties);
    return _this;
  }

  return VehicleTimeResponse;
}(PropertyResponse);

VehicleTimeResponse.identifier = [0x00, 0x50];
export default VehicleTimeResponse;