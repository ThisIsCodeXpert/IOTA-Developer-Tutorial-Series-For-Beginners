function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

import PropertyResponse from '../PropertyResponse';
import PropertyDecoder from '../PropertyDecoder';
import { switchDecoder } from '../helpers';

var TheftAlarmResponse = function (_PropertyResponse) {
  _inherits(TheftAlarmResponse, _PropertyResponse);

  /**
   * @property {String} theftAlarm (string 'not_armed|armed|triggered') Theft alarm state
   *
   * @example TheftAlarmResponse
    {
      theftAlarm: {
        value: 'triggered',
      },
    }
   */
  function TheftAlarmResponse(data, config) {
    _classCallCheck(this, TheftAlarmResponse);

    var _this = _possibleConstructorReturn(this, (TheftAlarmResponse.__proto__ || Object.getPrototypeOf(TheftAlarmResponse)).call(this));

    var properties = [new PropertyDecoder(0x01, 'theftAlarm').setDecoder(switchDecoder({
      0x00: 'not_armed',
      0x01: 'armed',
      0x02: 'triggered'
    }))];

    _this.parse(data, properties, config);
    return _this;
  }

  return TheftAlarmResponse;
}(PropertyResponse);

TheftAlarmResponse.identifier = [0x00, 0x46];
export default TheftAlarmResponse;