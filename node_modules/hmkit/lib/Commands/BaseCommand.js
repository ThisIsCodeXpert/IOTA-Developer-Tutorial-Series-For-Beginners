'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _encoding = require('../encoding');

var _helpers = require('../helpers');

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var BaseCommand = function () {
  function BaseCommand() {
    _classCallCheck(this, BaseCommand);
  }

  _createClass(BaseCommand, null, [{
    key: 'buildProperty',
    value: function buildProperty(identifier, value, encodingFunc) {
      var encodedValue = this.encodeProperty(value, encodingFunc);

      var dataComponent = [_encoding.PROPERTY_DATA_ID].concat(_toConsumableArray((0, _encoding.decimalToHexArray)(encodedValue.length, 2)), _toConsumableArray(encodedValue));

      return [identifier].concat(_toConsumableArray((0, _encoding.decimalToHexArray)(dataComponent.length, 2)), _toConsumableArray(dataComponent));
    }
  }, {
    key: 'encodeProperty',
    value: function encodeProperty(value, encodingFunc) {
      if (!!encodingFunc) {
        var encodedValue = encodingFunc(value);
        return (0, _helpers.isArray)(encodedValue) ? encodedValue : [encodedValue];
      }

      return (0, _helpers.isArray)(value) ? value : [value];
    }
  }]);

  return BaseCommand;
}();

exports.default = BaseCommand;